# 13 个 Open Issue 全修实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 GitHub 上 13 个 open issue,每个 issue 一个 commit,TDD 严谨执行。

**Architecture:** 按模块和文件依赖分组并行/串行执行。同文件的修复串行,不同文件可并行。

**Tech Stack:** NestJS 11, Prisma, class-validator, Jest

**Spec:** 13 个 issue body 已经在 GitHub 上描述清楚(每 issue 都有"模块/缺陷/复现/影响"),无需另写 spec。

---

## 依赖分析 + 分组

### Batch A(同文件,串行)— create-task.dto.ts 4 个 DTO 修复
- #46 (P0) contentConfig.language 缺 @IsEnum(ContentLanguage)  ← 5min
- #47 (P0) publishConfig.platform 缺 @IsEnum(Platform)         ← 5min
- #53 (P1) timezone 缺 @IsEnum                                 ← 5min
- #57 (P2) times 缺 per-item 校验                              ← 15min

**Subagent A(串行做完):** 1 个 subagent 修 4 个 DTO,总 30min

### Batch B(独立,可并行)
- #58 (P2) auto-publish.service toggleTask 中 registerTaskCron 未 await  ← 5min
- #48 (P0) Redis acquireLock + isKillSwitchActive 双 fail-open            ← 30min
- #56 (P2) notification 步骤过失败                                          ← 30min

**Subagent B/C/D:** 3 个 subagent 各自修 1 个

### Batch C(独立,可并行)
- #49 (P1) POST /auth/refresh 实现                                         ← 1-2h
- #50 (P1) timeToCron 仅匹配 HH:MM                                         ← 30min
- #51 (P1) Article 状态机 transition 校验                                  ← 1-2h
- #52 (P1) research P95 优化(并行化)                                       ← 2-3h
- #54 (P2) stories findAll 分页                                            ← 30min
- #55 (P2) DELETE stories 级联                                             ← 30min

**Subagent E/F/G/H:** 4 个 subagent 各自修 1-2 个

### 阶段
- 阶段 1(并行):Subagent A(串行 4 DTO) + Subagent B(#48) + Subagent C(#56) + Subagent D(#58)
- 阶段 2(并行):Subagent E(#49) + Subagent F(#50) + Subagent G(#51+#54+#55) + Subagent H(#52)
- 阶段 3:最终全测 + final review

---

## 全测要求

- 全部修复完后 `npx jest` 必须通过(目前 341/341)
- 不允许引入新失败
- 每个修复都要先写失败测试(TDD)

---

## Commit 命名

每个 issue 一个 commit,消息格式:
- `fix(#46): contentConfig.language @IsEnum 校验非法语言码`
- `fix(#47): publishConfig.platform @IsEnum 校验非法平台名`
- ...

---

## 最终验证

13 个 commit 在 `feat/fix-13-issues` 分支,push 后开 PR 到 main。
